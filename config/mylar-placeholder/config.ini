# Placeholder mounted only when MYLAR_DIR is unset. It deliberately contains
# no credentials. Configure a real read-only Mylar appdata mount or direct
# MYLAR_API_KEY and COMICVINE_API_KEY values before using requests/discovery.
