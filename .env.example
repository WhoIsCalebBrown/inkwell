# Marvel Developer API — https://developer.marvel.com
# Register, then copy this file to `.env`. Referer restrictions do not apply to
# server-side calls, but the private key must never reach the browser: the app
# only ever calls Marvel from the server.
MARVEL_PUBLIC_KEY=
MARVEL_PRIVATE_KEY=
